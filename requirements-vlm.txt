# Optional VLM branch (Phase 6, Moondream2). Kept OUT of the base env to stay light.
# Install only when you reach Phase 6:
#     uv pip install -r requirements-vlm.txt
#
# RESOLVED (2026-09): Moondream2 tương thích với transformers>=5 thông qua patch
# PreTrainedModel.all_tied_weights_keys trong prepare_models.py và scene_vlm.py.
# Moondream2 chạy mượt mà trên Apple Silicon MPS và CPU.
transformers
accelerate
einops
pillow
# torch / torchvision already come with the base env (via ultralytics)
